# Signing keys / secrets — NEVER commit
*.pem
*.keystore
*.jks

# Copyrighted game APKs — the tool patches a copy the user supplies; we ship none
*.apk
*.apk.idsig

# Python / build
.venv/
venv/
__pycache__/
*.pyc
build/
dist/
*.spec

# OS
.DS_Store

# keep the full-res logo master local; ship only the scaled header asset
assets/logo.png
